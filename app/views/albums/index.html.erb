<% if @user %>
  <div id="albums">
    <ul>
      <% @user.albums.each do |album| %>
        <li><%= link_to album.title, {:controller => :albums, :action => :show, :secret => album.secret} %></li>
      <% end %>
    </ul>
  </div>
<% end %>
